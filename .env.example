# ENVIRONMENT VARIABLES DOCUMENTATION
# Rename this file to .env.local and fill in your actual keys.

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Tomorrow.io Weather API
TOMORROW_API_KEY=your_tomorrow_api_key
NEXT_PUBLIC_TOMORROW_API_KEY=your_tomorrow_api_key

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin Credentials (Optional)
ADMIN_EMAIL=any-spoof-email
ADMIN_PASSWORD=admin123

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_key

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret


# PAYMENT GATEWAY - RAZORPAY (Primary for India)

NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_secret_key
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# Get test credentials from: https://dashboard.razorpay.com/
# 1. Sign up at: https://dashboard.razorpay.com/signup
# 2. Test Mode Keys: Dashboard → Settings → API Keys → Test Mode
# 3. Webhook Secret: Dashboard → Settings → Webhooks → Create Webhook
#
# Production Setup:
# - Switch to "Live Mode" in Razorpay dashboard
# - Replace test keys with live keys: rzp_live_xxxxx
# - Update webhook URL to production domain


# PAYMENT GATEWAY - STRIPE (Optional - Fallback/International)

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# Get test credentials from: https://dashboard.stripe.com/
# 1. Sign up at: https://dashboard.stripe.com/register
# 2. API Keys: Dashboard → Developers → API Keys
# 3. Webhook Secret: Dashboard → Developers → Webhooks → Add Endpoint
#
# Production Setup:
# - Use live keys (starts with pk_live_ and sk_live_)
# - Update webhook URL to production domain


# PAYMENT CONFIGURATION NOTES

# 1. Razorpay is PRIMARY gateway (recommended for Indian users - INR support)
# 2. Stripe is OPTIONAL fallback (for international payments)
# 3. You only need ONE gateway to start - Razorpay is sufficient
# 4. Webhook URLs must be HTTPS in production (HTTP ok for local testing)
# 5. For local webhook testing, use ngrok: https://ngrok.com/
#
# Local Webhook URL: http://localhost:3000/api/payments/webhook
# Production Webhook URL: https://your-domain.com/api/payments/webhook